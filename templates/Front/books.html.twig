{% extends 'base.html.twig' %}

{% block main %}
    <article>
        {% for book in books %}
            {% include'front/book.html.twig' %}
            <img class="article-img" src="{{ asset('uploads/images/'~ book.image ) }}" alt="{{ book.title }}">
        {% endfor %}
    </article>
{% endblock %}
